{
  "version": 2,
  "builds": [
    {
      "src": "app/main.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app/main.py"
    }
  ],
  "env": {
    "VERCEL_START_COMMAND": "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
  }
}
